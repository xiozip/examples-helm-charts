apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myjsapp.fullname" . }}-test-connection"
  labels:
    {{- include "myjsapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myjsapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
